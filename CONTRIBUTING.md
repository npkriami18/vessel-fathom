# Contributing

Fathom is a local-first CLI for interaction verification in live web apps.

## Development

```sh
corepack enable
pnpm install
pnpm run check
```

Keep changes focused. Packaging, CLI behavior, server behavior, and observer SDK behavior should include tests when they change user-visible behavior.
